{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Shop list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Contacts</th>
                <th>Description</th>
                <th>Longitude</th>
                <th>Latitude</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shop_crud_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.contacts }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.longitude }}</td>
                <td>{{ entity.latitude }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shop_crud_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shop_crud_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shop_crud_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
